# shop

Marketplace listings API.

- Data model: `src/models/item.js` (Mongoose).
- All item access goes through `src/data/itemRepository.js`.
- Routes are Express routers under `src/routes/`, mounted in `src/server.js`.
